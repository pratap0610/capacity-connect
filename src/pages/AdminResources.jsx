import { useEffect,useState } from 'react';
import { FileText } from 'lucide-react';
import PageHeader from '../components/PageHeader';
import ApiStatus from '../components/ApiStatus';
import { ErrorState,LoadingState,EmptyState } from '../components/LoadingState';
import { listResources } from '../services/resourceService';
export default function AdminResources(){const [r,setR]=useState(null),[error,setError]=useState('');async function load(){try{setR(await listResources())}catch(e){setError(e.message)}}useEffect(()=>{load()},[]);if(error&&!r)return <ErrorState message={error} onRetry={load}/>;if(!r)return <LoadingState message="Loading resource management data…"/>;return <div className="mx-auto max-w-7xl"><PageHeader eyebrow="Administration" title="Resource management" description="Review learning resources shared across the platform."/><ApiStatus source={r.source} message={r.message}/><div className="card mt-7 overflow-x-auto">{r.data.length?<table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-4">Resource</th><th>Subject</th><th>Type</th><th>Trainer</th><th>Date</th></tr></thead><tbody>{r.data.map(x=><tr className="border-t border-slate-100" key={x.id}><td className="px-5 py-4"><div className="flex items-center gap-3"><FileText size={17} className="text-brand"/><span className="font-bold">{x.title}</span></div></td><td>{x.subject}</td><td>{x.type}</td><td>{x.trainer}</td><td>{x.date}</td></tr>)}</tbody></table>:<EmptyState message="No resources are available."/>}</div></div>}
