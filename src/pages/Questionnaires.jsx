import { useEffect,useState } from 'react';
import { ClipboardList,Eye,Pencil,Plus,Trash2 } from 'lucide-react';
import { Link } from 'react-router-dom';
import PageHeader from '../components/PageHeader';
import ApiStatus from '../components/ApiStatus';
import { ErrorState,LoadingState,EmptyState } from '../components/LoadingState';
import { getQuestionnaires } from '../services/trainerService';
export default function Questionnaires(){const [r,setR]=useState(null),[error,setError]=useState('');async function load(){try{setR(await getQuestionnaires())}catch(e){setError(e.message)}}useEffect(()=>{load()},[]);if(error)return <ErrorState message={error} onRetry={load}/>;if(!r)return <LoadingState message="Loading questionnaires…"/>;return <div className="mx-auto max-w-6xl"><PageHeader eyebrow="Questionnaires" title="Assessment questionnaires" description="Create quick checks, set deadlines, and monitor submissions." action={<Link to="/trainer/questionnaires/create" className="btn-primary"><Plus size={17}/> Create questionnaire</Link>}/><ApiStatus source={r.source} message={r.message}/><div className="mt-7 space-y-4">{r.data.map(q=><div className="card p-5" key={q.id}><div className="flex flex-col gap-4 md:flex-row md:items-center"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-violet-50 text-purple"><ClipboardList/></div><div className="flex-1"><h2 className="font-black">{q.title}</h2><p className="mt-1 text-sm text-slate-500">{q.subject} • {q.questions} questions • Deadline {q.deadline}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{q.status}</span><div className="text-sm font-semibold text-slate-500">{q.submissions||0} submissions</div><div className="flex gap-2"><button onClick={()=>window.alert(`Submissions: ${q.submissions||0}`)} className="btn-secondary"><Eye size={15}/></button><button onClick={()=>window.alert('Edit mode is available through the questionnaire creation prototype.')} className="btn-secondary"><Pencil size={15}/></button></div></div></div>)}{!r.data.length&&<EmptyState message="No questionnaires are available."/>}</div></div>}
