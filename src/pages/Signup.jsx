import { Link, useNavigate } from 'react-router-dom';
import { ArrowLeft, CheckCircle2, Eye, EyeOff, LockKeyhole, Mail, UserRound } from 'lucide-react';
import { useState } from 'react';
import Logo from '../components/Logo';
import { signup } from '../services/authService';

export default function Signup() { const nav=useNavigate(); const [form,setForm]=useState({name:'',email:'',password:'',confirm:'',role:'trainee'}); const [show,setShow]=useState(false); const [error,setError]=useState(''); const [success,setSuccess]=useState(''); const [loading,setLoading]=useState(false); const update=(key,value)=>setForm(f=>({...f,[key]:value})); const strength=form.password.length>=12?'Strong':form.password.length>=8?'Good':'Weak';
  async function submit(e){e.preventDefault();setError('');setSuccess('');if(form.password!==form.confirm)return setError('Passwords do not match.');if(form.password.length<8)return setError('Password must be at least 8 characters.');setLoading(true);try{await signup(form);setSuccess('Account created successfully. You can now sign in.');setTimeout(()=>nav('/login'),700);}catch(err){setError(err.message);}finally{setLoading(false);}}
  return <div className="min-h-screen bg-[#f5f7fb] p-5"><div className="mx-auto max-w-5xl"><div className="flex items-center justify-between"><Logo/><Link to="/login" className="text-sm font-semibold text-slate-600">Already have an account?</Link></div><div className="mx-auto mt-10 max-w-2xl card p-7 sm:p-10"><Link to="/" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-500"><ArrowLeft size={16}/> Back home</Link><h1 className="mt-6 text-3xl font-black text-navy">Create your YOGYASETU account</h1><p className="mt-2 text-sm leading-6 text-slate-500">Choose Trainee or Trainer. Admin access is never created through public signup.</p><form className="mt-8 grid gap-5 sm:grid-cols-2" onSubmit={submit}><label><span className="text-sm font-semibold">Full name</span><div className="mt-2 flex rounded-xl border border-slate-200 bg-white px-3"><UserRound size={18} className="mt-3 text-slate-400"/><input value={form.name} onChange={e=>update('name',e.target.value)} required className="w-full bg-transparent px-3 py-3 outline-none" placeholder="Your full name"/></div></label><label><span className="text-sm font-semibold">Email</span><div className="mt-2 flex rounded-xl border border-slate-200 bg-white px-3"><Mail size={18} className="mt-3 text-slate-400"/><input value={form.email} onChange={e=>update('email',e.target.value)} required type="email" className="w-full bg-transparent px-3 py-3 outline-none" placeholder="you@example.com"/></div></label><label><span className="text-sm font-semibold">Password</span><div className="mt-2 flex rounded-xl border border-slate-200 bg-white px-3"><LockKeyhole size={18} className="mt-3 text-slate-400"/><input value={form.password} onChange={e=>update('password',e.target.value)} required type={show?'text':'password'} minLength="8" className="w-full bg-transparent px-3 py-3 outline-none" placeholder="At least 8 characters"/><button type="button" onClick={()=>setShow(v=>!v)} className="text-slate-400">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div><span className={`mt-1 block text-xs font-semibold ${strength==='Weak'?'text-orange-600':'text-success'}`}>Password strength: {strength}</span></label><label><span className="text-sm font-semibold">Confirm password</span><input value={form.confirm} onChange={e=>update('confirm',e.target.value)} required type="password" minLength="8" className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 outline-none" placeholder="Repeat password"/></label><div className="sm:col-span-2"><span className="text-sm font-semibold">Role</span><div className="mt-2 grid gap-3 sm:grid-cols-2">{[['trainee','Trainee','Build skills, learn, assess, and track competency.'],['trainer','Trainer','Teach, publish resources, and monitor performance.']].map(([value,title,desc])=><label key={value} className={`cursor-pointer rounded-xl border bg-white p-4 ${form.role===value?'border-brand ring-2 ring-blue-50':'border-slate-200'}`}><input type="radio" name="role" value={value} checked={form.role===value} onChange={e=>update('role',e.target.value)} className="mr-3"/><b>{title}</b><p className="mt-1 pl-6 text-xs text-slate-500">{desc}</p></label>)}</div></div><div className="sm:col-span-2 flex items-start gap-2 text-sm text-slate-500"><CheckCircle2 className="mt-0.5 text-success" size={18}/> Demo registrations are approved immediately; production deployments can enable administrator approval.</div>{error&&<div className="sm:col-span-2 rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</div>}{success&&<div className="sm:col-span-2 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm font-semibold text-success">{success}</div>}<button disabled={loading} className="btn-primary sm:col-span-2 w-full py-3 disabled:opacity-60">{loading?'Creating account…':'Create account'}</button></form></div></div></div>;
}
