export default function WeightedScoreBreakdown({result}){return <div className="card p-5"><div><div className="eyebrow">Transparent calculation</div><h2 className="mt-1 text-lg font-bold">Rule-Based Competency Score</h2><p className="mt-1 text-sm text-slate-500">Predefined evidence and weights — not a machine learning model.</p></div><div className="mt-5 space-y-4">{result.breakdown.map(x=><div key={x.key}><div className="flex justify-between text-sm"><span className="font-semibold">{x.label}</span><span className="text-slate-500">{x.score.toFixed(0)} × {(x.weight*100).toFixed(0)}% = <b className="text-slate-900">{x.contribution.toFixed(1)}</b></span></div><div className="mt-2 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-brand" style={{width:`${x.score}%`}}/></div></div>)}</div><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><span className="font-bold">Final Score</span><span className="text-2xl font-black text-brand">{result.score}%</span></div></div>}
