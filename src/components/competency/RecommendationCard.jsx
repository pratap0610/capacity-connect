import { ArrowRight, BookOpen } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
export default function RecommendationCard({item}){const nav=useNavigate();return <div className="card p-5"><div className="flex items-start gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-blue-50 text-brand"><BookOpen size={19}/></div><div className="min-w-0 flex-1"><div className="flex flex-wrap gap-2"><span className="rounded-full bg-red-50 px-2 py-1 text-[11px] font-bold text-red-700">{item.priority}</span><span className="text-xs text-slate-500">{item.skill}</span></div><h3 className="mt-2 font-bold text-slate-900">{item.course?.title || item.resource?.title}</h3><p className="mt-1 text-sm text-slate-500">{item.reason}</p></div></div>{item.course&&<button onClick={()=>nav(`/trainee/courses/${item.course.id}`)} className="btn-primary mt-4">Start Learning <ArrowRight size={16}/></button>}</div>}
