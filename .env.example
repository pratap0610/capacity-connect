# Set true for the frontend-only demo. Set false to use a real backend.
VITE_DEMO_MODE=true
# Never commit API keys or secrets to Vite environment variables.
VITE_API_BASE_URL=http://localhost:5000/api
VITE_API_TIMEOUT_MS=10000
